import css from './ProfileActivity.module.css';

function ProfileActivity() {
  return (
    <div className={css.wrapper}>
      <h2 className={css.title}>Profile Activity</h2>
    </div>
  );
}

export default ProfileActivity;
